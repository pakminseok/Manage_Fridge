<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.firstapp.managefridge">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_foreground_fridge"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_foreground_fridge"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.firstapp.managefridge.DashboardActivity"></activity>
        <activity android:name="com.firstapp.managefridge.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>